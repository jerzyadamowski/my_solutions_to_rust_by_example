pub fn example() {
  // uncomment to CALL panic and terminate program execution
  // panic!("This call never returns.");

  // unreachable_code below: comment to FIX me;
  // println!("You will never see this line!");
}
